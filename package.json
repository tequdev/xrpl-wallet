{
  "name": "xrpl-wallet",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "version": "lerna version --no-private",
    "publish": "lerna publish from-package"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
